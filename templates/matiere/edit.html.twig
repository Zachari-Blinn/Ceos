{% extends 'base.html.twig' %}

{% block title %}Editer Matiere{% endblock %}

{% block body %}
    <h1>Editer Matiere</h1>
    <br>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('matiere_index') }}">Classe_index</a></li>
            <li class="breadcrumb-item active" aria-current="page">Editer_matiere</li>
        </ol>
    </nav>

    {{ form_start(form) }}
        {{ form_widget(form) }}

    <div class="row">
        <div class="col">
            <a class="btn btn-primary" href="{{ path('matiere_index') }}">Retour</a>
            <button class="btn btn-success float-right">{{ button_label|default('Valider') }}</button>
        </div>
        <div>
            {{ form_end(form) }}
            {{ include('matiere/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
