<form method="post" action="{{ path('appreciation_generale_delete', {'id': appreciation_generale.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet objet ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appreciation_generale.id) }}">
    <button class="btn">Delete</button>
</form>
