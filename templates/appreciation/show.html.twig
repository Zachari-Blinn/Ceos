{% extends 'base.html.twig' %}

{% block title %}Appreciation{% endblock %}

{% block body %}
    <h1>Appreciation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appreciation.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ appreciation.Commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('appreciation_index') }}">back to list</a>

    <a href="{{ path('appreciation_edit', {'id': appreciation.id}) }}">edit</a>

    {{ include('appreciation/_delete_form.html.twig') }}
{% endblock %}
