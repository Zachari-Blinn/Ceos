{% extends 'base.html.twig' %}

{% block title %}Editer Eleve{% endblock %}

{% block body %}
    <h1>Editer Eleve</h1>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('eleve_index') }}">Eleve Index</a></li>
            <li class="breadcrumb-item active" aria-current="page">Editer Eleve</li>
        </ol>
    </nav>

    {{ form_start(form) }}
        {{ form_widget(form) }}

    <div class="row">
        <div class="col">
            <a class="btn btn-primary" href="{{ path('eleve_index') }}">Retour</a>
            <button class="btn btn-success float-right">{{ button_label|default('Valider') }}</button>
        </div>
        <div>
            {{ form_end(form) }}
            {{ include('eleve/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
